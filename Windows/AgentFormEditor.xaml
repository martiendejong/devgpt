<UserControl x:Class="DevGPT.AgentFormEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" d:DesignHeight="170" d:DesignWidth="800">
    <Border BorderBrush="Gray" BorderThickness="1" Margin="4" Padding="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!-- Name -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Name:" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" MinWidth="100" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" />
            </Grid>
            <!-- Description -->
            <Grid Grid.Row="1" Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Description:" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" MinWidth="300" Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" />
            </Grid>
            <!-- Prompt -->
            <Grid Grid.Row="2" Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Prompt:" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" MinWidth="300" Height="54" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding Prompt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch"/>
            </Grid>
            <!-- Stores (comma separated for now) -->
            <Grid Grid.Row="3" Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Stores (name:write, ...):" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" MinWidth="200" Text="{Binding StoresString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch"/>
            </Grid>
            <!-- Functions (comma separated) -->
            <Grid Grid.Row="4" Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Functions:" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" MinWidth="200" Text="{Binding FunctionsString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch"/>
            </Grid>
            <!-- CallsAgents (comma separated) and ExplicitModify -->
            <Grid Grid.Row="5" Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="120" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Calls Agents:" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" MinWidth="120" Text="{Binding CallsAgentsString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch"/>
                <CheckBox Grid.Column="2" IsChecked="{Binding ExplicitModify, Mode=TwoWay}" Content="Explicit Modify"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
